{
  "name": "pj-bot",
  "version": "1.0.0",
  "description": "Messenger Voice Bot with GitHub integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "lint": "eslint .",
    "setup": "cp config/config.example.json config/config.json && cp .env.example .env"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "request": "^2.88.2",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "eslint": "^8.56.0"
  }
}
